<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
  <title>New URL - TinyApp Example</title>
</head>
<body>
  <%- include('partials/_header') %>

  <main style="margin: 1em;">
    <h3>Create TinyURL</h3>
    <form class="form-inline" action="/urls" method="POST"> 
      <!-- the "action" tells the form which path to submit to while the "method" tells the form which HTTP method to use when submitting the form -->
      <div class="form-group mb-2">
        <label for="longURL">Enter a URL:</label>
        <input
          class="form-control"
          type="text"
          name="longURL"
          placeholder="http://"
          style="width: 300px; margin: 1em"
        />
        <!-- the name attribute in input identifies the data we are sending. It added the key "longURL" to the data we'll be sending in the body of our POST request, request.body.longURL = client input -->
        <!-- when this form is submitted, it will make a request to POST /urls, and the body will contain one URL-encoded name-value pair with the name longURL -->
        <button type="submit" class="btn btn-primary">Submit</button> 
        <!-- a button with a submit type, will submit the form data to the server -->
      </div>
    </form>
  </main>
 
</body>
</html>